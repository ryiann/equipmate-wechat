# EquipMate 微信小程序

> 企业设备全生命周期管理平台 — 微信小程序端

## 功能模块

| 模块 | 说明 |
|------|------|
| 🏠 首页 | 统计看板、快捷操作（扫码报修等）、待办工单列表 |
| 📦 设备台账 | 设备搜索/分类筛选、设备列表、详情查看、新增/编辑设备 |
| 🔧 维修工单 | Tab 状态切换、工单列表、创建报修、工单详情与操作（接单/完成/关闭） |
| 👤 个人中心 | 用户信息、统计数据、编辑资料、退出登录 |
| 💬 消息中心 | 消息列表、分类标签、点击跳转关联工单 |

## 角色权限

- **操作员 (USER)** — 报修、查看设备和工单进度、催办
- **技术员 (TECHNICIAN)** — 接单、维修、完成工单
- **管理员 (ADMIN/MANAGER)** — 派工、验收关闭、设备管理

## 项目结构

```
equipmate-wechat/
├── app.js / app.json / app.wxss    # 入口 + 全局配置 + 全局样式
├── project.config.json             # 项目配置
├── utils/
│   ├── request.js                  # wx.request 封装（含 Token、错误处理）
│   ├── api.js                      # 后端接口定义
│   ├── constants.js                # 状态映射 & BASE_URL 配置
│   └── util.js                     # 工具函数（时间、手机号脱敏等）
├── images/
│   ├── tabbar/                     # TabBar 图标 (8个)
│   └── icons/                      # 页面图标 (147个，7色 × 21种)
└── pages/
    ├── home/                       # 首页 (TabBar)
    ├── equipment/                  # 设备台账 (TabBar)
    ├── repair/                     # 维修工单 (TabBar)
    ├── profile/                    # 个人中心 (TabBar)
    ├── login/                      # 登录（手机号+验证码 / 微信授权）
    ├── equipment-detail/           # 设备详情
    ├── equipment-form/             # 设备新增/编辑
    ├── repair-detail/              # 工单详情（含时间线+维修记录）
    ├── repair-create/              # 创建报修
    ├── message/                    # 消息中心
    └── profile-edit/               # 编辑个人资料
```

## 快速开始

### 1. 配置

修改 `utils/constants.js` 中的后端地址：

```javascript
const BASE_URL = 'https://your-domain.com/api';
```

修改 `project.config.json` 中的小程序 AppID：

```json
"appid": "your-appid"
```

### 2. 运行

1. [下载微信开发者工具](https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html)
2. 打开项目目录 `equipmate-wechat/`
3. 编译运行

### 3. 服务器域名配置

在 [微信公众平台](https://mp.weixin.qq.com) → 开发管理 → 开发设置 → 服务器域名 中添加：

- **request 合法域名**：`https://your-domain.com`

> 开发阶段可在开发者工具中勾选「不校验合法域名」临时跳过。

## 后端接口

配套后端项目：[equipmate](../equipmate)（Spring Boot + MyBatis）

| 模块 | 接口 |
|------|------|
| 用户 | `POST /user/wx-login` · `POST /user/sms-login` · `GET /user/info` · `PUT /user/info` |
| 首页 | `GET /home/stats` |
| 设备 | `GET /equipment/list` · `GET /equipment/{id}` · `POST /equipment` · `PUT /equipment/{id}` · `POST /equipment/follow/{id}` |
| 工单 | `GET /repair/list` · `GET /repair/{id}` · `POST /repair` · `PUT /repair/{id}/start` · `PUT /repair/{id}/complete` · `PUT /repair/{id}/close` |
| 消息 | `GET /message/list` · `GET /message/{id}` |

## 技术栈

- **框架**：微信小程序原生（WXML + WXSS + JS）
- **网络**：封装 `wx.request`，Bearer Token 认证
- **登录**：微信授权登录 + 手机号验证码登录
- **优化**：按需注入 `lazyCodeLoading: requiredComponents`
